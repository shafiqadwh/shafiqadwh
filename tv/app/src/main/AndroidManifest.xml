<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET"/>
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE"/>
    <uses-permission android:name="android.permission.WAKE_LOCK"/>

    <!-- ทีวีไม่มีจอสัมผัสและไม่มี GPS ถ้าไม่ประกาศว่าไม่จำเป็น
         Google TV จะถือว่าแอปนี้ใช้กับทีวีไม่ได้ แล้วไม่ยอมติดตั้ง -->
    <uses-feature android:name="android.hardware.touchscreen" android:required="false"/>
    <uses-feature android:name="android.software.leanback" android:required="false"/>
    <uses-feature android:name="android.hardware.camera" android:required="false"/>
    <uses-feature android:name="android.hardware.location" android:required="false"/>
    <uses-feature android:name="android.hardware.microphone" android:required="false"/>

    <application
        android:allowBackup="false"
        android:banner="@drawable/banner"
        android:icon="@mipmap/icon"
        android:label="@string/app_name"
        android:networkSecurityConfig="@xml/network_security_config"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <!-- configChanges ยาวโดยตั้งใจ: ทุกอย่างที่ไม่ได้ประกาศไว้ทำให้ Android
             **สร้างหน้าจอใหม่ทั้งอัน** ซึ่งแปลว่าสไลด์โชว์เริ่มนับหนึ่งใหม่กลางงาน
             หน้าจอนี้เป็น WebView เต็มจอที่สร้างด้วยโค้ด ปรับตามขนาด/ความหนาแน่นได้เอง
             อยู่แล้ว จึงรับมือการเปลี่ยนแปลงพวกนี้เองได้โดยไม่ต้องสร้างใหม่ -->
        <activity
            android:name=".SlideshowActivity"
            android:configChanges="orientation|screenSize|keyboardHidden|smallestScreenSize|screenLayout|uiMode|density|fontScale|locale|layoutDirection|navigation|keyboard|touchscreen"
            android:exported="true"
            android:launchMode="singleTask"
            android:screenOrientation="landscape"
            android:theme="@style/AppTheme">

            <intent-filter>
                <action android:name="android.intent.action.MAIN"/>
                <!-- LEANBACK_LAUNCHER = ขึ้นบนหน้าโฮมของ Google TV
                     LAUNCHER = ติดตั้งบนมือถือหรือแท็บเล็ตเป็นตัวสำรองก็ยังเปิดได้ -->
                <category android:name="android.intent.category.LEANBACK_LAUNCHER"/>
                <category android:name="android.intent.category.LAUNCHER"/>
            </intent-filter>
        </activity>
    </application>
</manifest>
